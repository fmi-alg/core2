README for /lib

We keep various versions of the Core Library here:

E.g., If VAR=Debug when you "make core", you will get the library
	libcore++Debug.a  
for the static library.  If VAR=Opt, you get
	libcore++Opt.a, etc.

For the Core Extensions, we always make 3 levels of the library:
E.g., if VAR=NoOpt when you "make corex", you will get the library
	libcorex++NoOpt_level1.a
	libcorex++NoOpt_level2.a
	libcorex++NoOpt_level3.a


